
<div class="container">
    <ul class="list-group list-group-horizontal">
        <% Reaction.kinds.each do |kind| %>
            <li class="list-group-item justify-content-center align-items-center"> <%= @post.count_with_kind(kind) %> </li>
        <% end %>
    </ul>
</div>